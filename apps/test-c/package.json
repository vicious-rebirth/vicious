{
  "name": "@repo/test-c",
  "version": "0.0.0",
  "description": "Test for C codec",
  "scripts": {
    "setup:release": "cmake -S . -B build",
    "setup:debug": "cmake -DCMAKE_BUILD_TYPE=Debug -S . -B build",
    "build:exe": "cmake --build build -j 4"
  },
  "license": "MIT",
  "devDependencies": {},
  "dependencies": {
    "@repo/c": "workspace:*"
  }
}
