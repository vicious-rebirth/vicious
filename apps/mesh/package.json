{
  "name": "@repo/mesh",
  "version": "0.0.0",
  "description": "Tool to convert mesh files of Vicious Engine",
  "scripts": {
    "setup:release": "cmake -S . -B build",
    "setup:debug": "cmake -DCMAKE_BUILD_TYPE=Debug -S . -B build",
    "build:exe": "cmake --build build -j 4"
  },
  "license": "MIT",
  "devDependencies": {},
  "dependencies": {
    "@repo/c": "workspace:*"
  }
}
