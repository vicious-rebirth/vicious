{
  "name": "@repo/pack",
  "version": "0.0.0",
  "description": "Tool to package/unpackage assets for Vicious Engine",
  "scripts": {
    "setup:release": "cmake -S . -B build",
    "setup:debug": "cmake -DCMAKE_BUILD_TYPE=Debug -S . -B build",
    "build:exe": "cmake --build build"
  },
  "license": "MIT",
  "devDependencies": {},
  "dependencies": {
    "@repo/c": "workspace:*"
  }
}
